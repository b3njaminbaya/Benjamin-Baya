# Root-level env documentation
# Copy relevant variables into each app's .env file.
# See apps/web/.env.example and apps/api/.env.example for app-specific vars.

# Shared across apps (optional override)
NODE_ENV=development
